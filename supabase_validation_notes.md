# Supabase validation notes

- The only approved backend target for this task is the active, healthy project `aboyejoglobalfoods3` with reference `ljgwxgtizkguhbutrnxi` in `eu-north-1`.
- The six local migrations correspond to the six deployed migrations: the initial schema, authenticated admin grants, admin bootstrap, function hardening, legacy grant revocation, and private RLS helper access.
- The deployed public schema confirms RLS is enabled on the required business tables, `products.image_url` is the canonical product image field, and the product/category, product-image/product, order-item/product, and order/customer foreign-key relationships exist.
- The Storage schema reports the three expected bucket records. Direct authenticated verification confirmed that a temporary test image object was present immediately after upload and absent from the bucket listing after its associated record was deleted.
- The active backend is healthy. Its three image buckets are `product-images`, `gallery-images`, and `souvenir-images`; each is public for image delivery, permits JPEG/PNG/WebP files, and has a 10 MB object limit.
- RLS is enabled throughout the public commerce/content schema. Published storefront content is readable, public contact/newsletter writes are limited to inserts, and all business-record and Storage-object management policies are scoped to authenticated administrators through the private admin helper.
- The administrator entry renders with Supabase email/password sign-in and first-account creation controls. Its protected workspace contains product CRUD, stock and price controls, operations management, content management, direct file selection, and native drag-and-drop staging for image fields.
- The public storefront links visitors into the dedicated contact flow for enquiries, newsletter interaction, and WhatsApp details; the protected workspace exposes the corresponding operational-management tabs.
- A clearly temporary newsletter address was accepted through the live public contact form and displayed the successful subscription acknowledgement; it is being used only to verify the protected subscriber-management controls before deletion.
- The temporary subscriber appeared immediately in the protected `/admin` Operations workspace with its subscribed status and both Unsubscribe and Delete controls available.
- The administrator changed the temporary subscriber from subscribed to unsubscribed, then confirmed its deletion through the accessible in-page confirmation. The Operations workspace returned to the empty-subscriber state, leaving no temporary subscriber record behind.
- The upload paths are limited to `product-images`, `gallery-images`, and `souvenir-images`, and all product display/edit/storage code uses `image_url` as the single canonical product-image field.
- `pnpm install`, `pnpm check`, `pnpm test`, and `pnpm build` pass. The test suite has five passing tests, including Supabase configuration, image-field consistency, and administrator upload-management contracts. Post-restart public product requests return HTTP 200 under RLS, and the Supabase security advisor reports no findings. The remote performance-advisor request was temporarily unavailable, so the deployed index inventory was verified directly instead.
- The designated account successfully completed Supabase email/password sign-in and claimed the one-time first-administrator role in the live `/admin` workspace after the JWT-protected `admin-access` Edge Function was updated with browser CORS and preflight handling.
- A temporary administrator validation product was successfully created by dispatching a valid PNG through the live drag-and-drop image-field path. The live catalogue rendered its `product-images/products/1786664678564-validation-mark.png` public URL from the canonical `image_url` field. The in-page confirmation appeared on delete, the temporary product was removed, and an authenticated Storage listing returned no object for that exact path afterward. A public `HEAD` response may remain cached by the CDN, but the authoritative Storage listing confirms cleanup completed.
